PORT=5000
MONGO_URI=mongodb://localhost:27017/nimbus
JWT_SECRET=YOUR_JWT_SECRET_HERE
STRIPE_SECRET_KEY=YOUR_STRIPE_TEST_SECRET_KEY_HERE
CLIENT_URL=http://localhost:5173

# Copy this file to .env and fill in real values.
# Never commit a real .env file. Store secrets in a secure secret manager
# (e.g. Azure Key Vault) for anything beyond local development.
